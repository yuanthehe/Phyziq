<!DOCTYPE html>
<html>
  <body>
    <%= @appointment.summary %></br>
    <%= @appointment.event_start_time %></br>
    <%= @appointment.event_end_time %></br>
    <%= @appointment.address %></br>

    <%= button_to "delete", {:controller => :appointments,
      :action => 'destroy', :id => @appointment.id }, :method => :delete %>

  </body>
</html>
